function args = topoplot_args(h)

config = get_config(h);
args = {...   
    'Style',        get(config, 'Style'), ...
    'MapLimits',    get(config, 'MapLimits'), ...
    'NoseDir',      get(config, 'NoseDir'), ...
    'GridScale',    get(config, 'GridScale') ...
    'EMarker',      get(config, 'EMarker'), ...
    'HColor',       get(config, 'HColor'), ...
    'Shading',      get(config, 'Shading'), ...
    'WhiteBk',      get(config, 'WhiteBk') ...
    };

% Notes: 

% The Electrodes option is not considered here because, when calling to
% topoplot, we always want to plot the electrodes (in order to get a handle
% to them) and later, if necessary, hide them depending on the value of the
% Sensor property.

% 

end